{
  "buildCommand": "yarn build:www",
  "outputDirectory": "packages/apps/build",
  "installCommand": "yarn install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
